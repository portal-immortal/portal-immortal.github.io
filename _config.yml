# Minimal Jekyll config to exclude non-Jekyll project folders
exclude:
  - pptq
  - kanban
  - claude
  - claude-history
  - istwib
  - node_modules
  - src
  - public
  - "**/*.astro"

# Prevent using collections or other special processing
collections: {}

# Keep safe defaults
safe: true
future: false
verbose: false